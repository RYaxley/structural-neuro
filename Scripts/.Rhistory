basepath <- "~/Ares/RPV9/Results/7-Stats-c"
data <- read.csv(file.path(basepath, "Stats-RPV9.csv"))
# Load Duke P80 atlas data as baseline
atlas <- read.csv("~/Ares/RPV9/Tools/Atlas-Duke-P80-Johnson/Stats-Duke-Atlas.csv")
# Rename group factor so these data are not lumped together with the study Controls.
atlas$Group <- factor("Atlas", levels="Atlas")
# Merge datasets
data.atlas <- rbind(data,atlas) #data <- merge(data, subjects, by="Subject")
# Reorder labels
data$Group <- factor(data$Group,
levels=list("Unmanipulated", "Control", "AIE"))
data.atlas$Group <- factor(data.atlas$Group,
levels=list("Atlas","Unmanipulated", "Control", "AIE"))
se <- function (x) {
sqrt(var(x) / length(x))
}
# Create a copy of data with Unmanipulated and Control groups collapsed
data2 <- data
data2
data
getwd()
dir()
setwd("~/Ares/RPV9/Tools/Scripts/")
dir()
basepath <- "~/Ares/RPV9/Results/7-Stats-c"
data <- read.csv(file.path(basepath, "Stats-RPV9.csv"))
# Load Duke P80 atlas data as baseline
atlas <- read.csv("~/Ares/RPV9/Tools/Atlas-Duke-P80-Johnson/Stats-Duke-Atlas.csv")
# Rename group factor so these data are not lumped together with the study Controls.
atlas$Group <- factor("Atlas", levels="Atlas")
# Merge datasets
data.atlas <- rbind(data,atlas) #data <- merge(data, subjects, by="Subject")
# Reorder labels
data$Group <- factor(data$Group,
levels=list("Unmanipulated", "Control", "AIE"))
data.atlas$Group <- factor(data.atlas$Group,
levels=list("Atlas","Unmanipulated", "Control", "AIE"))
se <- function (x) {
sqrt(var(x) / length(x))
}
data2 <- data
require(car)
head(data2)
data2
data
View(data2)
recode(data2, "Unmanipulated = 'Control'")
?recode
recode(data2, "Unmanipulated" = "Control")
recode(data2$Group, "Unmanipulated"="Control")
recode(data2$Group, Unmanipulated="Control")
recode(data2$Group, Unmanipulated=Control)
levels(data2)
levels(data2$Group)
